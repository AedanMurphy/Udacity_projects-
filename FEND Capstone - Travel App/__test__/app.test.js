import getCount from '../src/Client/js/app'

describe('Testing funcionality', () => {
    it('should the getCount() function test that be true === true', () => {
      expect(getCount).toBe(getCount)
    })
  })